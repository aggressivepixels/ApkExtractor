package com.apkextractor.android.about.viewholders;

import android.support.v7.widget.RecyclerView;
import android.view.View;

/**
 * Simple {@link android.support.v7.widget.RecyclerView.ViewHolder RecyclerView.ViewHolder} that
 * shows an static {@link View}. Useful for headers, footers, section headers or
 * basically any kind of {@link View} that doesn't needs any setup and its completely static.
 *
 * @author Jonathan Hernández
 */
public class HeaderViewHolder extends RecyclerView.ViewHolder {
    public HeaderViewHolder(View itemView) {
        super(itemView);
    }
}
